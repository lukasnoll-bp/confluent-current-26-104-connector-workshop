apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prom-connect
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: true
    jsonData:
      httpMethod: POST
      timeInterval: 5s
